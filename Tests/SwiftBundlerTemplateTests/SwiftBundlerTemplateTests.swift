import XCTest
@testable import SwiftBundlerTemplate

final class SwiftBundlerTemplateTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftBundlerTemplate().text, "Hello, World!")
    }
}
